name: chat_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: any

dev_dependencies:
  dio: ^5.3.2
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  provider: ^6.0.5

flutter:
  uses-material-design: true
